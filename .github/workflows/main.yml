name: 🎭 Deploy CultGit

on:
  push:
    paths:
      - 'cultureness_home_updated/**'  # Cambiado de 'tools/culturenesshome/**'
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
      
      - name: ⎔ Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      
      - name: 📦 Install dependencies
        run: npm ci
        working-directory: ./cultureness_home_updated  # Cambiado
      
      - name: 🏗️ Build project
        run: npm run build
        working-directory: ./cultureness_home_updated  # Cambiado
      
      - name: 🚀 Deploy to CultGit
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.CULTGIT_TOKEN }}  # Token para CultGit
          external_repository: BlancaStudio/CultGit
          publish_dir: ./cultureness_home_updated/dist  # Cambiado
          keep_files: false
